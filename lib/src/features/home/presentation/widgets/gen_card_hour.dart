import 'package:flutter/material.dart';

class GenCardHour extends StatelessWidget {
  const GenCardHour({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(color: Colors.black,);
  }
}
